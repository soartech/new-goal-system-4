# Apply operator to mark goal achieved
sp "ngs*core*goal*apply*$NGS_REMOVE_ATTRIBUTE
  [ngs-match-selected-operator <s> <o> $NGS_REMOVE_ATTRIBUTE]
  (<o> ^dest-object     <dest>
       ^dest-attribute  <attr>
       ^value-to-remove <val>)
-->
  (<dest> ^<attr> <val> -)"